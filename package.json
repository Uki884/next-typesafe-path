{
  "name": "@safe-routes/monorepo",
  "private": true,
  "scripts": {
    "lint": "biome lint --apply ./",
    "format": "biome format --apply ./",
    "fix": "biome check --apply ./",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3"
  }
}